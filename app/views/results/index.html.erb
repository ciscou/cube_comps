<h1><%= @event.category.name %> results, <%= @round.name %></h1>

<% if @results.any? %>
  <%= render "results/results" %>
<% else %>
  <%= link_to "Start this round", new_event_round_path(@event) %>
<% end %>
